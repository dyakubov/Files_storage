1. До 1 Gb файлы передаются нормально, но потом передача резко замедляется. Подразумеваю, что связано это с тем,
что использую Files.write с опцией StandardOpenOption.APPEND для записи каждой части.

private void writeFileFromContainer(FileContainer fc) throws IOException {
        if (fc.getPart() == 0) {
            byte[] b = new byte[0];
            Files.write(Paths.get(userFolder + fc.getFileName()), b, StandardOpenOption.CREATE);
        } else {
            Files.write(Paths.get(userFolder + fc.getFileName()), fc.getData(), StandardOpenOption.APPEND);
        }
    }

2. Можно ли не заморачиваться пока с UI и на последний codereview сдать консольный вариант?